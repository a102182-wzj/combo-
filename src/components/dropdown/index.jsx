import React from 'react'
import { Dropdown } from 'antd'

export default class hmdropdown extends React.Component {
    render() {
        return (
            <Dropdown {...this.props} />
        )
    }
}